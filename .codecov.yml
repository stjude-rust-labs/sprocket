codecov:
  notify:
    after_n_builds: 9

coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

ignore:
  - "crates/gauntlet"

comment:
  behavior: once
  require_changes: "coverage_drop"
