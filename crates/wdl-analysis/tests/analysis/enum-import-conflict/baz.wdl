version 1.3

# This enum has the same choices but in a different order. This should
# conflict with the originally defined enum since order of choices matters.
enum Status {
    Pending = "pending",
    Active = "active"
}
