warning[OptionalInputSafety]: optional value optional_flag used in command placeholder without a guard
   ┌─ tests/lints/optional-input-safety/source.wdl:14:23
   │
14 │         samtools sort ~{optional_flag} ~{input_bam}
   │                       ^^^^^^^^^^^^^^^^
   │
   = fix: wrap the optional in `if defined(...) then ... else ...`, or use `select_first()` / `select_all()` so `None` is handled explicitly

warning[OptionalInputSafety]: optional value optional_flag used in command placeholder without a guard
   ┌─ tests/lints/optional-input-safety/source.wdl:27:24
   │
27 │         samtools sort "~{optional_flag}" ~{input_bam}
   │                        ^^^^^^^^^^^^^^^^
   │
   = fix: wrap the optional in `if defined(...) then ... else ...`, or use `select_first()` / `select_all()` so `None` is handled explicitly

warning[OptionalInputSafety]: optional value used in command placeholder without a guard
    ┌─ tests/lints/optional-input-safety/source.wdl:101:14
    │
101 │         echo ~{x + ""}
    │              ^^^^^^^^^
    │
    = fix: wrap the optional in `if defined(...) then ... else ...`, or use `select_first()` / `select_all()` so `None` is handled explicitly

